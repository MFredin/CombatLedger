# CombatLedger — Claude Code Instructions

## Git Workflow

You are authorized to run all git commands without asking for confirmation, including:

- `git add`, `git commit`, `git push`, `git pull`, `git fetch`
- `git tag`, `git branch`, `git checkout`, `git merge`
- `git push -u origin <branch>`

Always commit and push changes after completing a task unless explicitly told otherwise.

Use clear, descriptive commit messages that explain *why* the change was made.

## Branch

Active development branch: `claude/review-combatledger-addon-JEHDf`

Always push to this branch unless instructed otherwise.
